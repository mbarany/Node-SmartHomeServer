#!/bin/bash

BASEDIR=$(dirname $0)

git clean -f
git fetch --all
git reset --hard origin/master
git pull

npm install --prod
node $BASEDIR/dist.js
ln -sf $BASEDIR/../dist/crontab /etc/cron.d/Node-VeraSmartHomeServer
